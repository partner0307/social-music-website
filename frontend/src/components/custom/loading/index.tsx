import React from 'react';
import { LoadingContainer, SpinnerContainer } from './style';

const Loading = () => {
  return (
    <LoadingContainer>
      <SpinnerContainer />
    </LoadingContainer>
  );
};

export default Loading;
